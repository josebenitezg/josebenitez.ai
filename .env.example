# Optional. Likes are hidden and the site still builds when these are absent.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Keep false until joselo.blog serves the approved production deployment.
JOSELO_BLOG_REDIRECTS_ENABLED=false
